import styled from "styled-components";

const PostCardContainer = ({ className }) => {
  return (
    <div className={className}>
      <div>123</div>
    </div>
  );
};

export const PostCard = styled(PostCardContainer)`

`;